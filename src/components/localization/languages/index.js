import en from './en';
import tr from "./tr";
import de from "./de";

export const dictionaryList = {en, tr, de};

export const languageOptions = {
    en: 'English',
    tr: 'Türkçe',
    de: 'Deutsch'
}
